# Sass Configuration
SASS_PATH=node_modules:src
GENERATE_SOURCEMAP=true

# Use sass-embedded for modern API (no legacy warnings)
SASS_IMPLEMENTATION=sass-embedded

# API Configuration
REACT_APP_API_URL=http://localhost:8000
